
import { writable } from "svelte/store";

export const chat = writable(null);

export const scroll = writable(0);

export const reply = writable(null);

export const edit = writable(null);

export const change = writable(null);

export const read = writable(false);

export const menu = writable(null);

export const bottom = writable(true);

export const search = writable(null);
export const to = writable(null);
